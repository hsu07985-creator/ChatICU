name: server
description: ChatICU API Server built with Dart Frog
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  dart_jsonwebtoken: ^2.14.1
  uuid: ^4.5.1
  crypto: ^3.0.6

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.3
  test: ^1.25.5
